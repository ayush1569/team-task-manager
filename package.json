{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager backend and frontend configs",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd backend && npm install"
  }
}
